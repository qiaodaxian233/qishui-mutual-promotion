# 项目对接记忆（Project Handoff Memory）

> **给下一个 Claude（或未来的我）**：这个文件是 **qishui-mutual-promotion**（汽水音乐独立音乐人互推平台,对外品牌名"音乐加油站")项目的状态快照。
> 用户 qiaodaxian233 换对话时,把这个文件丢给你就能直接接着干。
> **第一件事**:读完这份文件再动代码。

> ⚠️ **关键警告(2026-05 回填教训)**:这个文档曾经停在 v0.5.2 整整十几个版本没更新,实际代码已经做到 v0.7.1+,**用户问"XX 功能改了没"时如果只看文档会答错**。
> 接到任何"是不是没做""有没有改过"的问题,**先做这一步**:
> ```bash
> cd /home/claude && rm -rf qishui && git clone https://github.com/qiaodaxian233/qishui-mutual-promotion.git qishui
> cd qishui && git log --oneline | head -30
> grep -rn "<关键词>" src/ frontend/src/ 2>/dev/null | head
> ```
> **再回答**。本仓库是 public,clone 不需要 PAT。每次会话开始都跑一遍,别信文档说的"还没做"。

---

## 👤 用户信息

- **GitHub**:[qiaodaxian233](https://github.com/qiaodaxian233)
- **仓库**:<https://github.com/qiaodaxian233/qishui-mutual-promotion>
- **默认分支**:`master`(参考用户其他项目偏好,不是 `main`)
- **生产部署**:宝塔面板 + PM2(待定具体路径)
- **沟通语言**:中文。**不喜欢冗长解释**,喜欢直接动手 + 简短结论
- **更新方式偏好**:`git pull` + `pm2 restart`,**不喜欢下载 zip 复制粘贴**
- **额度焦虑**:每个模块做完立刻 push,防止对话中断丢进度
- **文档同步铁律**(本轮血泪教训):**改完代码必须同步更新本文件 + 一起 push**。"代码进度领先文档"是上一阶段最大的债——别再这么干。

---

## 🎯 项目是什么

**汽水音乐独立音乐人互推平台** —— 不是刷量工具,是让小众独立音乐人**互相听见、互相点赞评论**的社区:

- **核心场景**:独立音乐人 A 把自己在汽水音乐的作品发布成任务,其他音乐人/听众用积分接单,去 A 的作品下点赞/评论/听完;A 也用同样方式帮别人。
- **价值观**:**真实互动,凭良心**。产品定位是"音乐发现 + 兴趣社交",互助作为辅助。
- **slogan 候选**:「一个让独立音乐人互相听见的地方,不是刷量工具。」

### 为什么不做刷量平台
1. **合规风险**:字节系风控严,纯刷量平台生命周期短、容易被认定为流量造假
2. **差异化**:细分独立音乐圈层有用户粘性
3. **道德底线**:用户原话「独立音乐人互推还得是凭良心」

---

## 📋 已确定的产品规则(用户拍板)

| # | 规则 | 类型 | 备注 |
|---|------|------|------|
| 1 | 新用户初始 **100 积分** | 经济 | 够发一两个小任务,逼用户接单赚积分 |
| 2 | 积分可买**置顶位**(加粗 + 🔥 标签) | 经济 | 建议 3 个位、24h、竞价机制 |
| 3 | **不能赚自己发布任务的积分** | 反作弊 | 后端 `task.publisher_id === user.id` 直接拒 |
| 4 | 注册必须**邮箱认证**(6 位验证码) | 准入 | 30 分钟有效期 |
| 5 | 一邮箱只能注册一个账号 | 准入 | `users.email` UNIQUE 索引 |
| 6 | **屏蔽临时邮箱**(2925/mailinator 等) | 准入 | 用 `disposable-email-domains` 仓库 |
| 7 | 同 IP 一首歌只能做一次任务 | 反作弊 | ⚠️ 见下方坑点,建议 IP + 设备指纹双因子 |
| 8 | 图片上传必须压缩 | 架构 | 服务器只有 **40G 硬盘** |
| 9 | 自动验证:**点赞 + 播放到指定秒数** | 验证 | 不强制评论 |

---

## 🏗️ 架构关键决策(不要打破)

### 1. 任务发布的输入是「分享链接文案」

发布者粘贴汽水音乐的分享文案,格式固定:
```
《Summer Haze》@汽水音乐 https://qishui.douyin.com/s/iQeg2PpD/
```

后端正则解析:
```javascript
const pattern = /《(.+?)》@汽水音乐\s+(https:\/\/qishui\.douyin\.com\/s\/([A-Za-z0-9]+)\/?)/;
// 提取:歌曲名、完整链接、短码(8 位)
```

**短码 `iQeg2PpD` 是天然唯一性 key**,但不同发布者分享同一首歌短码可能不同。所以平台级去重要拿到底层 `song_id`(从短链 HTTP 请求返回的 HTML 里解析)。

### 2. 验证机制的核心信号优先级

**最强 → 最弱**:
1. **分享链接服务端校验**(字节自己的服务器背书,几乎不可伪造)
2. **互动数差值校验**(任务发布前抓取点赞数 → 任务完成后再抓 → 差值校验,无需 OCR)
3. **OCR 截图核验**(歌曲名 / 点赞数 / 进度时间)
4. **行为风控**(同状态栏指纹、提交速度、活跃时段)

⚠️ **不要做账号代登录**。法律红线(《网络安全法》《刑法》285 条),只做用户手动操作 + 截图/链接验证。

### 3. 积分经济模型

```
积分流入(新用户赠送 + 活动) < 积分流出(任务消耗 + 平台抽成)
```

- **平台抽成 10%**:发布者扣 10,接单者得 9,1 进平台池(销毁)
- **不开放积分充值**(开了就变黑灰产引流工具,法律风险大)
- **积分有效期**:90 天不活跃过期(防僵尸积分堆积)
- **置顶位**:积分消耗的核心场景

### 4. 信用分 ≠ 积分(两套独立)

- **积分是货币**(可消耗)
- **信用分是身份**(决定权益)

| 信用分 | 权益 |
|--------|------|
| < 500 | 仅能接单,不能发任务 |
| 500-700 | 可发任务,接单上限 5/天 |
| 700-850 | 任务上限放开,可参与争议仲裁 |
| > 850 | 高级权益、专属任务、平台合作 |

### 5. 图片生命周期(40G 硬盘的关键)

| 阶段 | 处理 |
|------|------|
| 上传 | 压缩为 720px WebP(`sharp` / `Pillow`),~40-80 KB |
| 验证通过后 7 天 | 保留缩略图供争议查阅 |
| 7 天后 | 删除文件,只保留 OCR 结果 + 图片 SHA256(查重) |
| 30 天后 | 任务彻底归档 |

**强烈建议**:图片走对象存储(七牛云/阿里云 OSS),本地只放数据库 + 代码。

### 6. 同 IP 限制的坑

中国手机用户大量经过运营商 NAT,几百用户共用出口 IP。硬卡"同 IP 一首歌一次"会**误伤真用户**。

**推荐方案**:IP + 设备指纹(FingerprintJS)双因子
- 同 IP + 同设备 → 拦截(确认同一人)
- 同 IP + 不同设备 → 放行(不同用户同 WiFi)
- 不同 IP + 同设备 → 拦截(切 IP 作弊)

---

## 🔌 任务类型(MVP 只做这些)

| 任务类型 | 必填字段 | 验证依据 | 优先级 |
|---------|---------|---------|--------|
| 点赞 | 分享链接 | 互动数差值 + OCR 截图 + 24h 回查 | **P0** |
| 播放(指定秒数) | 分享链接、最短时长 | OCR 进度时间 + 截图时间戳差 | **P0** |
| 评论 | 分享链接、文案要求 | 评论链接爬取 + 48h 回查 | P1 |
| 分享 | 分享链接 | 短码格式校验 + 短码查重 | P1 |
| 加歌单、关注 | — | 截图 + 回查 | P2(后期) |

---

## 📂 项目结构(实际,截至 v0.7.1+)

```
qishui-mutual-promotion/
├── package.json               ← 后端依赖(version: 0.7.1)
├── ecosystem.config.js        ← PM2 配置
├── .env.example
├── .gitignore                 ← .env / node_modules/ / uploads/screenshots/
├── README.md
├── 项目对接记忆.md             ← 本文件
├── db/
│   ├── schema.sql             ← 主表结构
│   └── migrations/
│       ├── v0.3.2.sql         ← IP 字段改 CHAR(64)
│       ├── v0.5.6.sql         ← 截图凭证字段
│       ├── v0.5.7.sql         ← 福利任务字段
│       └── v0.6.0.sql         ← 通知 / 信用分 / 管理员
├── scripts/
│   ├── init-db.js
│   ├── import-disposable-domains.js
│   ├── backup-db.sh           ← 数据库备份
│   ├── dedup-collect.js       ← 截图收集池去重
│   ├── ocr-collect-check.js   ← 截图收集池 OCR 审核(tesseract.js)
│   ├── ocr-collect-purge.sh
│   └── reset-points-to-100.sql ← 一次性脚本:重置非管理员积分
├── src/
│   ├── server.js              ← 后端入口
│   ├── config/
│   │   ├── index.js           ← 环境变量集中加载
│   │   └── db.js              ← MySQL 连接池
│   ├── routes/
│   │   ├── auth.js
│   │   ├── tasks.js
│   │   ├── completions.js
│   │   ├── points.js
│   │   ├── checkin.js         ← 每日签到(7 天阶梯 5/7/9/12/15/20/30)
│   │   ├── notifications.js   ← 站内通知
│   │   ├── admin.js           ← 管理员接口(用户管理/任务审核/调积分/全员发积分)
│   │   └── collect.js         ← 截图收集池(/collect 公开页用)
│   ├── services/
│   │   ├── auth.js
│   │   ├── tasks.js
│   │   ├── completions.js
│   │   ├── points.js
│   │   ├── credit.js          ← 信用分独立服务
│   │   ├── songs.js
│   │   ├── qishui-parser.js   ← 分享链接解析
│   │   ├── qishui-interactions.js ← 互动数实时刷新
│   │   ├── notifications.js   ← 通知服务
│   │   ├── scheduler.js       ← 定时任务(过期/超时/24h 回查)
│   │   ├── email-verification.js
│   │   ├── mailer.js          ← QQ SMTP + 失效告警
│   │   ├── storage.js
│   │   ├── local-verify.js    ← 本地 OCR 验证(tesseract.js + 红心/进度条像素检测)
│   │   └── screenshot-ai.js   ← Claude API 视觉识别截图
│   ├── utils/
│   │   ├── crypto.js
│   │   ├── validate.js
│   │   ├── request.js
│   │   └── image.js           ← sharp 图像处理(压缩/裁剪/灰度/锐化)
│   └── middlewares/
│       ├── auth.js
│       └── rate-limit.js
├── frontend/                  ← Vue 3 + Vite + Vant 前端源码
│   ├── package.json           ← (version: 0.7.0)
│   ├── vite.config.js
│   ├── postcss.config.cjs     ← px → vw 自动转换
│   ├── public/
│   │   ├── logo.png
│   │   ├── collect.html       ← 截图收集页(独立,绕开 Vue 打包)
│   │   └── example-correct.jpg
│   └── src/
│       ├── main.js
│       ├── App.vue
│       ├── api/index.js       ← axios 拦截器
│       ├── router/index.js
│       ├── stores/user.js     ← Pinia + localStorage 持久化
│       ├── composables/useTheme.js  ← 自动暗色模式(prefers-color-scheme)
│       ├── styles/global.css  ← 汽水绿 #1AFE49 + Apple 风变量
│       └── views/
│           ├── HomeView.vue
│           ├── LoginView.vue
│           ├── RegisterView.vue
│           ├── ForgotPasswordView.vue
│           ├── AgreementView.vue
│           ├── TasksView.vue        ← 任务广场
│           ├── TaskDetailView.vue   ← 任务详情(可编辑/接单/审核)
│           ├── PublishTaskView.vue  ← 发布任务
│           ├── MyView.vue           ← 「我的」首页(底部 Tab)
│           ├── ProfileView.vue      ← 个人资料
│           ├── PointsView.vue       ← 积分明细
│           ├── NotificationsView.vue
│           ├── AdminDashboardView.vue ← 管理后台
│           └── AdminWelfareView.vue ← 福利任务批量发布
├── public/                    ← 前端构建产物(入 git)
│   ├── index.html
│   ├── logo.png
│   ├── collect.html
│   └── assets/
└── uploads/
    └── screenshots/           ← 用户截图(.gitignore)
```

**技术栈已定(下面这些就是现状,不要打破)**:
- 后端:Node.js + Express/Koa vs Go + Gin
- 数据库:MySQL + Redis(任务队列 + 缓存)
- OCR:百度云 / 阿里云(0.001-0.01 元/次)
- 邮件:腾讯企业邮箱 SMTP / SendGrid
- 对象存储:七牛云 / 阿里云 OSS

---

## 📅 开发路线图(MVP 顺序)

- [x] **阶段 0**:仓库初始化 + .gitignore + README + 本文件 push
- [x] **阶段 1**:数据库表结构 SQL(users / tasks / completions / points_log / pinned_slots / disposable_domains)
- [x] **阶段 2**:用户注册 + 邮箱认证 + 临时邮箱屏蔽
- [x] **阶段 3**:任务发布(分享链接解析 + 元数据抓取)
- [x] **阶段 4**:接单 + 凭证上传(图片压缩 + OCR)
- [x] **阶段 5**:自动验证(互动数差值 + 24h 回查 + 截图凭证 + OCR + AI 截图分析)
- [x] **阶段 6**:积分系统 + 置顶竞价(三档置顶:金/七彩/超级)+ 每日签到(7 天阶梯)
- [x] **阶段 7**:风控(设备指纹 + 接单冷却 + 每日限量 + 阶梯冷却 + 一人一次)
- [x] **阶段 8**:前端 Vue 3 + Vant(玩家页 + 发布页 + 个人中心 + 积分明细 + 通知 + Apple 暗色风)
- [x] **阶段 9**:管理后台(任务审核 + 用户管理 + 举报处理 + 调积分 + 全员发积分 + 数据统计)
- [x] **阶段 10**:邀请系统(邀请码 + 50 积分奖励) + 找回密码
- [x] **阶段 11**:截图收集页 `/collect`(为 OCR 训练 + 红心/进度条检测调参)
- [ ] **下一步**:看用户反馈再定(可能方向:对象存储迁移 / Redis token / 反作弊评分细化)

---

## ⚠️ 已知避坑

### A. 永远不要做账号代登录
法律红线。不收集汽水账号密码,只让用户在自己设备上手动操作。

### B. 不开放积分充值
开了就变现金交易,变黑灰产引流工具,法律风险极大。

### C. OCR 规则要配置化
汽水音乐 UI 会改版,OCR 字段提取的位置/格式规则要做成**配置表**,不要硬编码。

### D. 浏览器缓存
前端 css/js 改了,用户报问题先让他 Ctrl+F5。

### E. 不要替字节当警察
平台只要做到"不被一眼看穿是黑产工具"就够了。**适度宽松验证 + 严厉事后追责**比严格事前验证效果更好。

### F. 提示文案要清楚
"为什么我的邮箱不能注册""为什么这个任务做不了"要有 FAQ。清晰的规则比严苛的规则更留人。

---

## 🔄 PAT 推送工作流(重要)

每次用户给 PAT 让你推 GitHub 时,**严格按这个流程**:

```bash
# 1. 临时目录 clone(PAT 只在 URL 里,不进 git config)
mkdir -p /tmp/push_work && cd /tmp/push_work
PAT="github_pat_xxx"
git clone "https://x-access-token:${PAT}@github.com/qiaodaxian233/qishui-mutual-promotion.git" 2>&1 | sed 's/[A-Za-z0-9_]\{40,\}/<REDACTED>/g'

# 2. 拷贝改动文件
cd qishui-mutual-promotion
cp /home/claude/qishui-mutual-promotion/server.js ./server.js
# ... 其他改动文件

# 3. 配置 git 身份
git config user.name "qiaodaxian233"
git config user.email "qiaodaxian233@users.noreply.github.com"

# 4. commit + push
git add -A
git commit -m "feat: <模块名>"
git push 2>&1 | sed 's/[A-Za-z0-9_]\{40,\}/<REDACTED>/g'

# 5. 清理
cd / && rm -rf /tmp/push_work
```

**所有输出**都要用 `sed 's/[A-Za-z0-9_]\{40,\}/<REDACTED>/g'` 过滤,防止 PAT 泄漏到对话历史。

---

## 🚦 用户偏好(Communication Style)

- **直接给方案,不要长篇大论**。先动手,结论放最后
- **被质疑能力时坦诚承认**比辩解好(参考 truth-dare-wheel 经验:沙箱白名单里就有 github.com,PAT 推送完全可行,别说"我没法用")
- 喜欢**对比表格**和**进度清单**
- 提的功能要**一次性全做完**,别漏
- 部署用 `git pull`,**不喜欢下载 zip**
- **额度焦虑明显**:每做完一块立刻 push,别攒一大堆
- 碰到下载链接挂掉**直接走 PAT git push**,别绕路

---

## ⚠️ 下一个 Claude 行动检查清单

**用户明确要求**:当用户问"我们之前是不是做过 XX"或"有没有类似的"时,**先在沙箱里查本地代码状态**,再回答。

```bash
# 1. 看本地有没有相关代码
grep -rn "<关键词>" /home/claude/qishui-mutual-promotion/ | head -20

# 2. 对比本地和远端
cd /tmp && rm -rf check-remote && git clone --depth 1 https://github.com/qiaodaxian233/qishui-mutual-promotion.git check-remote 2>&1 | tail -2
diff /tmp/check-remote/server.js /home/claude/qishui-mutual-promotion/server.js | head -40

# 3. 然后再回答
```

**别**只搜过往对话就说"没做过" —— 沙箱里可能有没推到 GitHub 的半成品。

---

## 📝 此文件维护

下一次 Claude 做了重大改动,请:

1. 更新版本号(package.json `version` 对应)
2. 在「版本历史」加一条
3. 更新「项目结构」段反映新文件
4. **推送本文件到 GitHub**(让记忆持续)

文件路径:`项目对接记忆.md`(仓库根目录)

---

## 📜 版本历史

### v0.7.2 — 运维 hardening:启动横幅动态化 + tessdata 自检 + uploads 可写自检 + credit_log.reason 字段统一(2026-05-20)

**起因**:用户反馈"签到给了 846 积分"——查证发现服务器还在跑旧代码(初版 v0.6.4 随机加权积分,846 落在 5% 大奖区间)。`pm2 restart` 后修好,但日志暴露出几个隐藏 bug。

**改动文件**(6 修改 + 2 新增):

| 文件 | 改动 |
|---|---|
| `src/server.js` | 启动横幅 `v0.5.2` 硬编码 → 改读 `package.json.version`(永久解决不同步) |
| `src/server.js` | 启动时增加 **tessdata 自检** + **uploads 目录可写性自检**,缺失给清晰告警(不阻塞启动) |
| `src/services/local-verify.js` | `getWorker()` 加 `checkTessdata()` 预检 + `workerInitFailed` 标记(避免每次请求重试失败的初始化) |
| `db/schema.sql` | `credit_log.reason` 从 **ENUM(6 个英文值)** → **VARCHAR(500)**,跟 migration 和实际代码对齐 |
| `db/schema.sql` | `idx_reason` 改前缀索引 `reason(64)`(VARCHAR(500) 完整索引会超 utf8mb4 索引长度限制) |
| `db/migrations/v0.7.2.sql` | **新增**,生产服务器升级用(ENUM → VARCHAR 无损转换) |
| `scripts/install-tessdata.sh` | **新增**,一键下载 `chi_sim.traineddata + eng.traineddata`(因为 tessdata/ 在 .gitignore) |
| `.env.example` | 加 `CLAUDE_API_KEY`(可选,AI 截图分析用)+ 品牌名从"汽水音乐互推平台"统一改成"音乐加油站" |
| `package.json` | version `0.7.1` → `0.7.2` |

**4 个隐藏 bug 的根因**(给下一个 Claude 当案例库):

1. **启动横幅 v0.5.2 不更新**
   - 根因:`src/server.js:120` 硬编码字符串,跟着 commit 历史一直没改
   - 修复:改成 `require('../package.json').version`
   - 教训:**任何版本号字符串都必须从 package.json 读**,不要硬编码

2. **tessdata 缺失导致 OCR 失败**
   - 根因:`.gitignore` 第 50 行 `tessdata/`,但 `2d1ebe0` commit 改成"本地模型文件不在线下载",代码指向 `./tessdata` 路径——`git pull` 拉不到模型文件
   - 报错形式:`Error opening data file ./����raineddata`(乱码是 PM2 日志缓冲压坏了 UTF-8,实际是 `chi_sim.traineddata`/`eng.traineddata`)
   - 修复:写下载脚本 + 启动自检 + worker 失败标记
   - 教训:**任何被 gitignore 的运行时依赖,必须配套提供下载/安装脚本** 并在启动时检测

3. **credit_log.reason ENUM 与代码对不上**
   - 根因:schema.sql 主表里 reason 是 ENUM 6 个英文值,但 v0.6.0.sql migration 写的是 VARCHAR(200),代码 `credit.js` 实际传中文自由文本('接单超时未提交' 等)
   - 报错形式:`Data truncated for column 'reason' at row 1`(**ENUM 不匹配,MySQL 也报 "Data truncated",跟字符串长度无关**)
   - 修复:schema + migration 统一改 VARCHAR(500)
   - 教训:**migration 和 schema.sql 必须双向同步**,任何字段定义变更两边都要改

4. **uploads/collect 目录权限**
   - 根因:PM2 运行用户(可能是 root)和宝塔默认目录权限(www:www)冲突
   - 报错形式:`Permission denied`,`unable to open for write`
   - 修复:启动时自检 + 告警 + 自动 mkdir(权限问题仍需手工 chown)

**生产部署步骤(v0.7.1 → v0.7.2 升级)**:

```bash
cd /www/wwwroot/qishui-mutual-promotion
git pull origin master

# 跑 migration(如果服务器 credit_log 之前是 ENUM 才必跑)
mysql -u <user> -p <db_name> < db/migrations/v0.7.2.sql

# 装 tessdata(如果 tessdata/ 目录没有或缺文件)
bash scripts/install-tessdata.sh

# 修 uploads 权限(如果启动日志告警)
chown -R $(whoami) uploads/

# 重启
pm2 restart qishui-mutual-promotion

# 验证启动横幅显示 "音乐加油站 v0.7.2"
pm2 logs qishui-mutual-promotion --lines 20
```

**给下个 Claude 的提醒**:
- 接到"为什么我的 XX 跟你说的不一样"问题时,**先看启动日志**——版本号是不是真生效了,有没有告警条
- v0.7.2 的启动自检是好东西,以后加任何**外部资源依赖**(模型文件 / 数据库 migration / 配置文件)都应该在 server.js 启动钩子里加自检告警

---

### v0.7.3 — 邀请奖励调整:1000 → 50 积分(2026-05-20)

**起因**:邀请好友得 1000 积分过于慷慨,降低到 50 积分以保持积分经济平衡。

**改动文件**(2):

| 文件 | 改动 |
|---|---|
| `src/services/auth.js` | 邀请奖励 SQL UPDATE + INSERT 三处 `1000` → `50`,注释同步更新 |
| `frontend/src/views/MyView.vue` | 邀请卡文案「邀请好友得 1000 积分」→「邀请好友得 50 积分」 |

---

### v0.7.1+ — v0.7.1 → v0.7.2 之间的中间状态归档(2026-05)

> ⚠️ **回填说明**:从 v0.7.1 到 v0.7.2 之间,有 **30+ 个 commit 没按版本号严格切片**,改动太密集。这一节按"功能簇"归纳所有未版本化的提交。这一段功能在 master 上是 v0.7.1 到 v0.7.2 之间累积形成的。

**当时 master 实际状态**:`package.json: 0.7.1` / `frontend/package.json: 0.7.0` / 最新 commit `f503b21`(被 v0.7.2 后续 commit 取代)

**关键改动簇(按时间倒序)**:

1. **签到改阶梯方案**(`116c99f`)
   - **之前 v0.6.4 是"随机 50~1000 积分加权概率"**,生产实测过于慷慨且不可预期
   - 改为连续阶梯 `STREAK_REWARDS = [5, 7, 9, 12, 15, 20, 30]`,7 天为一周期
   - **断签清零**:`getStreakBeforeToday` 从昨天往前数,有断点就归 0
   - 周满日(第 7/14/21... 天)发"周满奖励"通知 + `isWeekBonus` 标识
   - 文件:`src/routes/checkin.js` + `frontend/src/views/HomeView.vue` 签到卡

2. **截图收集池 + OCR 离线审核**(`5805ccc` → `f503b21`)
   - 公开页 `/collect`(`frontend/public/collect.html`),设备指纹防重复,目标收集 90 张样本
   - **全分辨率保存(不压缩)** 用于训练
   - 收集页加截图示范(像素扫描精确定位标注框)
   - 离线审核脚本 `scripts/ocr-collect-check.js`,从系统包 tesseract 改 **`tesseract.js`** 复用项目依赖,无需 `dnf install tesseract`
   - 配套备份脚本 `scripts/backup-db.sh` + 去重 `scripts/dedup-collect.js`

3. **OCR + 红心 + 进度条检测大幅改进**(10+ 个 fix)
   - 红心检测:兼容紫色/深色主题、阈值提到 0.5%、暖色调兼容、精确裁切区域
   - 进度条检测:**圆点定位法**(放弃整条扫描)、宽扫描区+y跨度过滤、**按分辨率分档**(tall/medium/short 三套参数)、桶聚类+孤立簇过滤、扫描区缩到 87-91%、簇宽限 3-25px、放宽颜色阈值支持浅灰白圆点
   - OCR 图像预处理:裁剪 + 放大 + 灰度 + 锐化
   - **所有任务类型都检测进度条(防秒赞秒评)**
   - 歌名不匹配直接拒绝(防张冠李戴)
   - 3 次重试 + 人工审核申请 + OCR 超时保护(超时 10s)

4. **找回密码**(`9578038`):完整前端 `ForgotPasswordView.vue` + 后端接口

5. **改名"音乐加油站" + SMTP 失效告警**(`f980c85`)
   - **品牌从"汽水互推"改成"音乐加油站"** —— 更柔性,弱化"互推"的工具感
   - QQ SMTP 失效(授权码过期/被腾讯封)时发管理员告警

6. **AI 截图分析(Claude API 视觉识别)**(`e046fa7`)
   - 新增 `src/services/screenshot-ai.js`
   - 本地 OCR + AI 双线验证,本地拒绝时可走 AI 兜底

7. **本地 OCR 模型离线化**(`2d1ebe0`):`tesseract.js` 改用本地模型文件,不再在线下载(部署稳定性)

8. **接单流程加固**:
   - 3 次失败才显示人工审核按钮(`8a689ea`),每次失败都记录 proof 并计数
   - 截图验证失败可重新提交(`45e95e0`)
   - **简化分享格式 - 只需粘贴链接,歌名自动抓取**(`f71e407`)
   - 接单状态持久化、截图失败可重试
   - 截图上传改用 label+for 方案修复手机点击无反应(`725a557`)

9. **置顶系统进化**(`d89c9ec` → `aa618ad`)
   - v0.6.5 单档置顶:50 积分,金色流光歌名
   - 进化为 **三档**:金色 / 七彩 / 超级
   - 新发布任务排在置顶下面(按时间倒序,不再按福利优先)(`b3417fd`)

10. **任务编辑权限**(`50481cf`):任务详情可编辑奖励/名额/延期(仅发布者)

11. **邀请好友系统**(`a351034`):邀请码 + 50 积分奖励

12. **防作弊系统**(`d9edf01`):综合行为打分 + 设备指纹 + IP 联动

13. **接单限量与冷却**:
    - 每日限量接单 - 防平台风控(`c38d7d4`)
    - 接单冷却间隔 - 任务被接后暂时消失(`fbb0cfc`)
    - 阶梯冷却 + 编辑冷却/限量(`36acd3d`)
    - 严格一人一次接单(`dd5bfe2`)

14. **互动数据实时刷新 + 智能频率**(`86cd074` + `bdaa9ed`):有人用快刷,没人用慢刷

15. **Token 持久化到数据库 + 记住账号**(`9618f23`):跨重启不掉登录(**取代 v0.2.0 的内存 Map 方案**)

16. **未登录可浏览任务广场和详情**(`241209d`):降低注册门槛

17. **分享文案解析兼容无空格**(`cb1f3f9`):`@汽水音乐https://...` 也能解析(汽水复制按钮在某些版本会吃掉空格)

18. **一次性脚本**(`d552862`):`scripts/reset-points-to-100.sql` - 把非管理员积分重置到 100(冷启动后清场用,**已用过,别再跑**)

19. **历史 fix**(已合入主线):
    - `points_log` enum 漏 `daily_checkin` + `task_pin`(`721069e`)
    - scheduler 重复声明 refund 导致启动崩溃(`7a5037e`)
    - tasks.js pool 未引入 + 清理重复 require(`c8d8f9a`)

**给下个 Claude 的提醒**:

- 已部署的 DB 如果是 v0.6.5 之前建的,务必跑 `db/migrations/v0.6.0.sql` 以及 enum 补丁(看 `721069e`)
- 截图收集页 `/collect` 的目标(90 张)是配置,达成后可关闭——别让它一直公开收集
- AI 截图分析走 Claude API 是**额外成本**,本地 OCR 通过的优先不调 AI
- 签到 7 天阶梯参数 `STREAK_REWARDS` 配置化容易改,**不要硬编码到前端**——前端从 `/api/checkin/status` 的 `weekRewards` 读

---

### v0.7.1 — Apple 风格 + 自动暗色模式 + 注册/登录页重构(2026-05)

commits:`ecfaaeb`(v0.7.0)+ `47348a5`(v0.7.1)+ `a51e91a` `cbfee6e` `236def0` `8b7cd78`

**改了什么**:
- 全局 CSS 改 Apple 简约风(白色卡片 + 微妙阴影 + 大圆角)
- `composables/useTheme.js` 自动暗色模式(跟 `prefers-color-scheme`)
- 注册/登录页重构 + 用户协议页 `AgreementView.vue`
- 首页 Logo 居中、置顶卡片绿色呼吸光效

**踩坑**:
- 签到卡在暗色模式下文字不可读 → 改用 `--card-bg` 变量(`a51e91a`)
- `logo.png` 必须放在 `frontend/public/` 不是 `frontend/src/`,Vite 构建才能正确处理

---

### v0.6.5 — 置顶功能(2026-05)

commit `d89c9ec` + 关联 enum 修复 `721069e`

- 50 积分置顶任务(初版,后来 v0.7.1+ 改三档)
- 金色流光歌名(CSS 动画)
- **重要**:`points_log.type` 之前没加 `task_pin`,导致置顶时插流水报错——`721069e` 补了 enum,**老库要 ALTER TABLE**

---

### v0.6.4 — 每日签到(初版)+ 管理员调积分/全员发积分(2026-05)

commit `5b41ff5` + `02916a0`(fix)

- **初版签到**:首页签到卡,随机 50~1000 积分加权(50% 50-100 / 30% 100-300 / 15% 300-600 / 5% 600-1000)—— **后来 `116c99f` 改成阶梯,见 v0.7.1+ 段**
- 管理员调积分:用户列表每行加「调积分」按钮
- 全员发积分:管理后台快捷按钮,一键给所有活跃用户发指定积分
- fix:`getUserById` 漏返回 role 字段,导致管理员菜单不显示(`02916a0`)

---

### v0.6.2 — 管理后台快捷按钮 + 「我的」页菜单上移(2026-05)

commit `4b9876b`

- 管理后台加快捷按钮(待审核数量徽章)
- 「我的」页菜单从底部移到顶部(更易触达)

---

### v0.6.1 — 任务卡片显示互动数据 + 放宽限流(2026-05)

commit `1e74821`

- 任务广场卡片直接显示点赞/评论/分享数(从 `qishui_interactions` 抓的实时)
- 放宽 API 限流(原来太严,真用户被误伤)

---

### v0.6.0 — 管理后台 + 消息通知 + 个人资料 + 信用分 + 数据统计(2026-05)

commit `0c489ac` + migration `db/migrations/v0.6.0.sql`

**新增模块**:
- 后端:`src/routes/admin.js` + `src/routes/notifications.js` + `src/services/credit.js`(**信用分独立服务**)+ `src/services/notifications.js`
- 前端:`AdminDashboardView.vue` / `NotificationsView.vue` / `ProfileView.vue`

**核心**:管理后台第一版上线,**任务审核 / 用户管理 / 举报处理 / 数据统计** 四大功能。

---

### v0.5.7 — 管理员批量发布福利任务(2026-05)

commit `9f2c88b` + migration `db/migrations/v0.5.7.sql`

- 福利任务:**管理员发布,系统出钱**,所有用户可接,鼓励冷启动
- `tasks` 表加 `is_welfare` 字段
- 前端 `AdminWelfareView.vue`

---

### v0.5.6 — 接单提交必须上传截图凭证(2026-05)

commit `1fc753c` + migration `db/migrations/v0.5.6.sql`

**重大补丁,推翻 v0.4.0 的决策**:

v0.4.0 当时说"OCR/截图全部不要,只用互动数差值"——但生产实测发现**互动数差值容易被作弊**(刷了又取消、刷别人评论顶替),所以**回退到需要截图凭证**——但保留互动数差值作为辅助信号。

- `completions` 表加 `proof_url` 字段
- 接单提交时必传截图
- 后续 v0.7.1+ 各种 OCR/红心/进度条检测都建立在这一步之上

---

### v0.5.5 — 积分明细页 + 定时任务(2026-05)

commit `d7a7a52`

- 前端 `PointsView.vue` 积分流水分页
- `scheduler.js` 增强:任务过期 / 接单超时(48h 未提交自动取消并返还名额)

---

### v0.5.4 — 底部 Tab 导航 + 「我的」页(2026-05)

commit `26296bc`

- App 风格底部 Tab:任务 / 发布 / 我的
- `MyView.vue` 个人中心入口(头像/积分/信用分/我发的/我接的/通知/设置)

---

### v0.5.3 — 修按钮/标签颜色 + 发布任务页 + 接单流程(2026-05)

commit `73cfa6c`

- Vant 按钮在亮绿背景下文字色调整
- 完整发布任务页 `PublishTaskView.vue`(粘贴文案 → 预览 → 提交)
- 接单流程前端串通(领取 → 去汽水操作 → 提交 → 审核)

---

### v0.5.2 — 阶段5.2 任务广场 + 任务详情 + UI 完善(2026-05)

**本阶段产出**:

**UI 完善(commit 1)**:
- `frontend/src/views/HomeView.vue` —— brand-emoji 加 96px 汽水绿圆底
  问题:Windows/Mac/Android 的 🎵 是彩色 emoji(紫色),CSS color 改不动
  解法:绿圆底覆盖,emoji 还是音符但视觉变成"绿色圆 + 紫色音符",品牌识别强
- `frontend/src/styles/global.css` —— 补 `--van-tag-primary-*` 系列
  Vant 4 标签有独立变量,`--van-button-*` 不辐射
  改成"淡绿底 + 深绿字"(`rgba(26,254,73,0.15)` + `#0EBA37`),
  标签是次要信息(信用分、状态),不能跟主按钮一样炫

**任务广场列表(commit 2)**:
- `frontend/src/views/TasksView.vue` —— Vant `van-pull-refresh` + `van-list`
  下拉刷新 + 触底无限滚动
- 顶部 5 个 tab(全部/点赞/播放/评论/分享)
- 卡片信息:封面、歌名、歌手、类型 tag、奖励/次、剩余名额、发布者、相对时间
- 封面图加载失败有 emoji 降级(`@error` 替换 DOM)
- 空状态:已登录用户显示"发布第一个任务"CTA(发布页是 5.4)
- `onActivated` 钩子:keep-alive 回到本页时静默刷新顶部(更新动态字段
  如 `quota_remaining`,不破坏用户滚动位置)
- HomeView 把"任务广场即将上线"占位换成真实入口卡片

**任务详情页(commit 3)**:
- `frontend/src/views/TaskDetailView.vue` —— 4 卡堆叠
  1. 歌曲信息:封面、歌名(2 行截断)、歌手、时长(MM:SS)
  2. 任务信息:类型/奖励/剩余名额/最少听秒(仅 listen 类型显示)/截止时间/发布者
  3. 分享链接:只读 + 一键复制
     - 优先用 `navigator.clipboard.writeText`(需 HTTPS 或 localhost)
     - 降级用 `execCommand('copy')`(HTTP 环境也能跑)
- 4. 怎么接单步骤说明
- 底部 fixed 操作栏(桌面端 480px 居中):
  - 自己发的 → "撤销我发布的任务"(`van-dialog` 确认 + 调 `/api/tasks/:id/cancel`)
  - 已招满 → 禁用"已招满"
  - 其他 → "接单"按钮(占位,5.5 实现)
- 路由 `/tasks/:id(\d+)` 限制 ID 必须数字,非法 id 被 SPA fallback 兜底到 /

**关键设计决策**:
1. **任务广场不过滤"自己发的"** —— 列表上看得到,但点进详情时按钮变成"撤销"。
   这样发布者能在广场上看到自己任务的曝光位置(理解竞争 + 决定要不要追加积分)
2. **复制链接的双方案**(clipboard API + execCommand)—— 服务器现在是 HTTP 直连
   (`http://IP:3201`),`navigator.clipboard` 在非 HTTPS 下会拒绝。必须有 fallback
3. **任务卡片移动端体验**:`active` 状态做 `scale(0.98)` 微动效,有点击反馈
4. **底部操作栏 fixed**:桌面端要跟 `.app-root` 一起 480px 居中,
   用 `position:fixed; left:50%; transform:translateX(-50%)` 实现

**Vue Router 用法新增**:
- `/tasks/:id(\d+)` 正则参数 —— 限制 id 必须是纯数字
- `onActivated` —— 给 keep-alive 用,本项目当前**没启用 keep-alive**,
  但留着不影响,未来加 `<keep-alive>` 时直接生效

**API 调用清单**:
- `GET /api/tasks?type=X&limit=20&offset=N` —— 任务广场列表(已有,5.2 仅前端调用)
- `GET /api/tasks/:id` —— 任务详情(已有)
- `POST /api/tasks/:id/cancel` —— 撤销(已有,详情页调用)

**没做的(放到后续阶段)**:
- 接单流程(`POST /api/tasks/:id/claim`)→ **阶段 5.5**
- 发布任务页 → **阶段 5.4**
- 个人中心(我发的/我接的/积分流水)→ **阶段 5.6**

### v0.5.1 — 阶段5.1 登录/注册页 + 汽水绿主题(2026-05)

**重要背景**:这一次有个"事故" —— 上一轮 Claude 写完 5.1 代码但没拿到 PAT 推送,
新会话沙箱重置后代码丢失。这一轮从仓库 v0.5.0 状态重写。**教训记下来**:
**写完一段就推一段,不要攒**。再额度焦虑也不要在沙箱里囤超过 30 分钟的代码。

**本阶段产出**:
- `frontend/src/styles/global.css` —— 主色从 `#ff6b35` 橙改为 `#1AFE49` 汽水绿
  - 关键决策:Vant 主按钮文字用 `#0a0a0a` 深色(白字在亮绿上对比度太差)
  - 链接、`--van-primary-color` 用 dark 版 `#0EBA37`,正文里可读
- `frontend/src/views/HomeView.vue` —— health-indicator 从 `position:fixed` 改 `absolute`,
  锚到 `.app-root`(它在桌面端是 max-width:480 居中容器),
  之前桌面截图里那个"奇怪绿色半圆"就是这个问题
- `frontend/src/views/LoginView.vue` —— 完整登录页:
  - Vant Form + onBlur 校验
  - 调 `/api/auth/login`,写 Pinia store + localStorage
  - 支持 `?redirect=/xxx`,防开放重定向(redirect 必须以 / 开头)
- `frontend/src/views/RegisterView.vue` —— 完整注册页:
  - 邮箱 → 验证码(60s 倒计时,只在邮箱合法时才允许发)
  - 密码强度前端做和后端 `isValidPassword` 完全对齐(8-64 + 字母 + 数字)
  - 用户协议必勾才能提交,弹窗显示 5 条核心条款
  - 注册即登录(后端 register 接口直接返回 token)
- `public/*` —— 重新构建,382 模块,gzip 后 ~88KB

**关键决策**:
1. **主色选高饱和荧光绿 `#1AFE49` 而不是 Material Design 风格的稳重绿** —— 用户明确表示
   "汽水音乐是绿色的"(黑底荧光绿 logo),稳重绿失去品牌识别度
2. **按钮上文字用深色** —— 白色文字在 `#1AFE49` 上对比度只有 ~1.8:1,WCAG 都不到 AA。
   深色文字 14:1 对比度,反而更"潮"更"汽水"
3. **倒计时按钮的可点状态由 computed 控制**(`canSendCode`),
   必须邮箱合法 + countdown=0 + 没在发送中,三条都成立才允许点
4. **密码前端校验和后端完全对齐**,避免前端通过后端报错的尴尬体验
5. **注册成功后的 redirect**:登录/注册都支持 `?redirect=`,但**强制以 / 开头**防开放重定向

**Vant 主题覆盖踩坑**:
- Vant 4 用 `--van-button-primary-background` 这种细粒度变量,不是 `--van-primary-color` 一键全改
- 全局 CSS 里这几条要同时设:`--van-button-primary-background` / `--van-button-primary-border-color` / `--van-button-primary-color`(文字色),否则会出现"按钮变色了但边框还是蓝色"的怪状态

**API 不变**:阶段 5.1 完全前端工作,不动后端代码(除了启动日志版本号)

### v0.5.0 — 阶段5.0 前端工程初始化(Vue 3 + Vite + Vant)(2026-05)

**技术栈定案**:
- 前端框架:**Vue 3 + Composition API + `<script setup>`**
- 构建工具:**Vite 6**
- UI 组件库:**Vant 4**(字节系移动端组件,中文文档,跟汽水风格匹配)
- 状态管理:**Pinia**(替代 Vuex,Composition API 风格)
- 路由:**Vue Router 4**(history 模式)
- HTTP:**axios** + 拦截器
- CSS 适配:**postcss-px-to-viewport-8-plugin**(375 设计稿,px → vw)

**部署方式**:
- 前端构建产物直接输出到根目录的 `public/`,Express 静态托管
- 前端走 `/` 等路径,后端 API 走 `/api/*`
- SPA fallback:所有非 `/api/*` 的 GET HTML 请求都返回 `index.html`,让 Vue Router 接管
- **`public/` 入 git**(简化部署,产物体积小)
- **`frontend/node_modules/` 不入 git**

**项目结构新增**:
```
qishui-mutual-promotion/
├── frontend/                  ← 前端源码
│   ├── index.html
│   ├── package.json
│   ├── vite.config.js
│   ├── postcss.config.cjs     ← px → vw 自动转换
│   └── src/
│       ├── main.js            ← Vue 入口
│       ├── App.vue            ← 根组件,只放 router-view
│       ├── api/index.js       ← axios + 拦截器(401 自动跳登录)
│       ├── router/index.js    ← 路由 + 守卫
│       ├── stores/user.js     ← Pinia 用户 store(localStorage 持久化)
│       ├── styles/global.css  ← 全局样式 + CSS 变量(汽水橙 #ff6b35)
│       └── views/
│           ├── HomeView.vue       ← 首页(已实现:健康检查、登录态切换、登出)
│           ├── LoginView.vue      ← 占位,阶段 5.2 实现
│           └── RegisterView.vue   ← 占位,阶段 5.2 实现
└── public/                    ← 构建产物(入 git)
    ├── index.html
    └── assets/
        ├── *.js (gzipped ~70KB)
        └── *.css (gzipped ~88KB)
```

**关键决策**(下一个 Claude 必读):

1. **Vue 单页应用 + 后端静态托管,一个端口出**(3201)
   - 优点:不用单独跑前端服务,不用 nginx 加 location
   - 注意:server.js 的静态托管必须在所有 `/api/*` 路由之后

2. **前端构建产物入 git**
   - 用户部署只需 `git pull + pm2 restart`,不需要在服务器跑 npm build
   - 开发流程:本地改 frontend/ → `npm run build` → `git add public/` → push

3. **px → vw 自动转换**(375 设计稿基准)
   - postcss-px-to-viewport-8-plugin 配置在 `postcss.config.cjs`
   - 写 CSS 时直接写 `font-size: 14px`,构建后自动变成 `vw`
   - 黑名单:`.no-vw-` 前缀的选择器不转换;`node_modules/vant` 不转换(Vant 已自适应)

4. **桌面端贴合策略**(用户偏好「手机优先,桌面贴合小屏」)
   - global.css 里 `@media (min-width: 768px)` 把整个 app 限制在 480px 居中
   - 桌面端看起来就像"手机模拟器",符合用户要求

5. **登录态用 localStorage 持久化**
   - token + user JSON 都存 localStorage(参考 truth-dare-wheel v2.6.0 的经验)
   - 跨 tab 共享,关浏览器不失效

6. **Vant 组件按需自动引入**
   - 无需手动 `import { Button, Cell } from 'vant'`
   - 用 unplugin-vue-components + VantResolver,直接写 `<van-button>` 就行
   - 但**样式还是要手动 import 一次**:`import 'vant/lib/index.css'`(main.js 里已 import)

7. **axios 拦截器自动处理 401**
   - 任何接口返回 401,前端 clearAuth + 跳转 /login
   - 用 `location.href` 而不是 `router.push`,确保彻底重置应用状态

8. **健康检查 indicator**
   - 首页右下角小标签,显示「● 服务正常」/「● 服务异常」
   - 调 `GET /api/health`,前后端连通性一眼看出来
   - 开发完所有页面后可以撤掉,目前留着方便调试

**API 不变**:阶段 5.0 不动后端,只是给后端加了静态托管

**测试通过**:
- 沙箱内 `npm install` 成功(89 个包,62MB)
- `npm run build` 成功:372 模块转换,产物 ~88KB gzipped
- `node src/server.js` 启动后没有 `public/ 不存在` 警告,证明静态托管正确挂载

**部署步骤**(给用户):
```bash
cd /www/wwwroot/qishui-mutual-promotion
git pull
pm2 restart qishui-mutual-promotion
# 然后用浏览器访问 http://服务器IP:3201/ 应该能看到首页
```

**待办**(交给下一个 Claude / 子阶段):
- 阶段 5.1:发布任务页(粘贴分享文案 + 预览解析 + 提交)
- 阶段 5.2:登录 / 注册页(完整 form + 邮箱验证码倒计时)
- 阶段 5.3:任务广场 + 任务详情
- 阶段 5.4:接单 + 完成流程
- 阶段 5.5:个人中心(我发的、我接的、积分流水、信用分)

**注意事项**(下一个 Claude):
- 写新页面时,**先在沙箱 npm run build 跑通**再推
- 修改 vite.config.js 后记得在沙箱测试构建,避免推送到服务器才发现配置错
- public/ 是构建产物,**不要手动改 public/ 里的文件**,改 frontend/ 然后构建
- Vant 组件中文文档:https://vant-ui.github.io/vant/

### v0.4.0 — 阶段4 接单 + 自动验证(互动数差值,纯服务端)(2026-05)

**重大决策**:**完全跳过 OCR 和截图上传**,改用"互动数差值校验"方案:
- 用户决定:`OCR 方案选哪个? → 先不接 OCR,先只做互动数差值验证`
- 阶段 3 的预览解析已经能从汽水分享页抓真实的 likes/comments/shares(实测从 48 涨到 49,数据实时)
- 既然能服务端抓互动数,就不需要用户上传截图,**用户体验更好、零存储成本、反作弊更强**

**新增模块**:
- `src/services/completions.js` — 接单 + 提交 + 24h 回查
- `src/services/scheduler.js` — 简易定时任务(setInterval,无外部依赖)
- `src/routes/completions.js` — completion 查询和操作接口

**改动模块**:
- `src/routes/tasks.js` — 新增 `POST /:id/claim` 接单接口
- `src/server.js` — 挂载 completions 路由 + 启动 scheduler

**API 清单**(本版本新增):
| 路径 | 方法 | 鉴权 | 说明 |
|------|------|------|------|
| `/api/tasks/:id/claim` | POST | ✅ | 接单(body 或 X-Device-Fp 头需带 deviceFp)|
| `/api/completions` | GET | ✅ | 我接的任务列表 |
| `/api/completions/:id` | GET | ✅ | 接单详情 |
| `/api/completions/:id/submit` | POST | ✅ | 提交完成(自动验证) |
| `/api/completions/:id/recheck` | POST | ✅ | 立即回查(调试用) |

**核心流程**:

```
1. claim(领取):
   - 校验任务存在/活跃/有名额/非自己发布
   - 抓当前互动数 → 存 baseline 快照(snapshot_type=task_create)
   - 行锁扣 quota_remaining, 创建 completion(状态=claimed)
   - 唯一约束: 同 task+ip+device 不能接两次, 同 task+user 不能接两次

2. submit(提交完成):
   - 非接单者本人不能提交
   - 防脚本秒过:至少 10 秒后才允许提交
   - 再抓一次互动数 → 存 task_complete 快照
   - 比对 like/comment/share 增量 ≥ 1 → auto_passed,设 recheck_at = now + 24h
   - 增量 < 1 → auto_rejected,quota_remaining +1 名额返还
   - ⚠️ 此时**不发积分**,等 24h 回查通过才发

3. recheck(24h 后回查):
   - scheduler 每 10 分钟扫一次 recheck_at 到期的
   - 再抓一次 → 数没掉(currVal >= completedVal) → 发积分,状态=manual_passed
   - 数掉了(用户撤销点赞) → 状态=recheck_failed,不发积分
   - TODO: recheck_failed 时扣接单者信用分(阶段 5/6)
```

**关键决策**(下一个 Claude 必读):

1. **OCR/截图/文件存储本阶段全没有**。用户体验:点"接单" → 去汽水点赞 → 回来点"完成"。

2. **like 任务的"点赞"按钮在汽水分享页 UI 上叫"收藏"**(❤️ 图标)。提示文案要写清楚:"请点击歌曲页右侧的 ❤️ 收藏(点赞)按钮"。

3. **deviceFp 是接单必传的**,前端用 FingerprintJS 算,通过 body.deviceFp 或 X-Device-Fp header 传。没传直接拒绝。本阶段先严格,后续考虑匿名 fallback。

4. **积分双阶段发放**:submit 通过 ≠ 发积分,recheck 通过才发积分。反作弊关键 —— 用户没办法"点赞 → 拿积分 → 取消点赞"。

5. **隐含 bug 未处理**:`tasks.cancelTask` 撤销任务时,如果有 completion 是 `auto_passed` 但还没 awarded,这部分接单者的努力没退还。**留 TODO 给阶段 5/6**。

6. **scheduler 是 setInterval 实现**,每 10 分钟跑一次。**PM2 必须 fork 模式(instances:1)**,cluster 模式会多实例重复执行。上量后切 node-cron + Redis。

7. **回查批量上限 50**,同步循环避免压垮汽水。队列堆积可加 sleep。

**待办**(交给下一个 Claude / 阶段):
- 撤销任务的高级退款(处理 auto_passed 未 awarded)
- 信用分扣减(recheck_failed)
- 评论任务关键词校验(需爬取评论列表)
- listen 任务的自动验证(汽水没暴露播放次数,可能需要回退到截图)
- token 切 Redis

### v0.3.2 — 修复 IP 字段长度 + dotenv 绝对路径(2026-05)

**用户实际部署时发现的两个 bug**:

1. **`Data too long for column 'register_ip'`** — 我把 `register_ip` 建成 `VARCHAR(45)`(够 IPv6),但实际存的是 64 位 SHA256 hash,溢出。
   - 修:`register_ip` / `last_login_ip` / `email_verifications.ip` 全改 `CHAR(64)`
   - `email_verifications.ip` 之前存原始 IP,**现在统一存 hash**,所有 IP 不留原始

2. **PM2 启动时找不到 .env** — `dotenv.config()` 默认 `process.cwd()`,PM2 启动时 cwd 不是项目目录。
   - 修:`config/index.js` 用绝对路径 `path.join(__dirname, '..', '..', '.env')` 加载

**新增**:
- `db/migrations/v0.3.2.sql` — 给已部署的库做 ALTER TABLE 升级(不需要重建表)

**用户部署验证**:实测注册流程跑通,user_id=1,points=100,token 返回。

### v0.3.1 — qishui-parser 基于真实 HTML 精确化(2026-05)

用户提供了真实的汽水分享页数据,我把解析器从"多策略 fallback 猜结构"重写成"精确匹配 + fallback 兜底"。

**关键发现**(下一个 Claude 必读):

1. **短链 → 长链的 URL 里直接有 track_id**
   - 短链:`https://qishui.douyin.com/s/iQeg2PpD/`
   - 重定向后:`https://music.douyin.com/qishui/share/track?track_id=7637175882045982771&...`
   - 直接 `new URL(finalUrl).searchParams.get('track_id')` 就拿到了 → **不用解析 HTML JSON**

2. **SSR HTML 结构稳定**:
   - 歌名:`<h1 class="title">Summer Haze</h1>`
   - 歌手:`<span class="artist-name-max">渡微</span>`
   - 封面:`<img alt="a-image" src="...">` (alt 是稳定标识)
   - 互动数:`<div class="action-container"><div class="action-icon"><img alt="收藏/评论/分享"><span class="number">数字</span></div></div>`
   - 歌词:`<div class="ssr-lyric">每行</div>`

3. **⚠️ 重要业务认知**:汽水分享页上"收藏"按钮 = 用户点的"喜欢/点赞"
   - alt 写的是"收藏",但业务含义是点赞
   - 代码里 `收藏 | 点赞 | like` 都映射到 `likes` 字段
   - 这条**影响所有以"点赞"为名的任务和验证逻辑** —— 接单者要点的就是"收藏"按钮

4. **歌词 SSR 已经能拿到**(对接记忆里说过的"反作弊王牌"基础就绪)
   - 后续阶段 5 自动验证时,可以用歌词时间轴 + 进度条交叉校验

**改动文件**(1 个):
- `src/services/qishui-parser.js` 完全重写(基于真实结构)

**测试**:
- 用户提供的真实 HTML 跑了 6 组测试,**全部命中**
- 歌名/歌手/封面/歌词 8 行/点赞 48/评论 26/分享 2/track_id `7637175882045982771` 全部正确提取
- 数字解析支持 "48" / "1.2万" / "3.5w" / "1.5亿" / "1,234" 等格式

**API 不变**(对外接口稳定,只是内部命中率从 ~50%(猜)提升到 ~100%(精确))

### v0.3.0 — 阶段3 任务发布 + 分享链接解析 + 积分托管(2026-05)

**新增模块**:

- `src/services/qishui-parser.js` — 汽水分享链接解析(纯文本 + HTTP 抓页 + 多策略元数据提取)
- `src/services/songs.js` — 歌曲表 upsert + 24h 同歌去重
- `src/services/points.js` — 积分扣款/加款,事务化,行锁防并发
- `src/services/tasks.js` — 任务发布主流程(校验 → 解析 → 扣款 → 入库 → 互动快照)
- `src/routes/tasks.js` — 任务相关路由
- `src/routes/points.js` — 积分查询路由

**API 清单**(本版本新增):

| 路径 | 方法 | 鉴权 | 说明 |
|------|------|------|------|
| `/api/tasks/config` | GET | ❌ | 任务类型配置(reward 区间等) |
| `/api/tasks/preview` | POST | ✅ | 预览解析(不扣积分) |
| `/api/tasks` | POST | ✅ | 发布任务(扣积分) |
| `/api/tasks` | GET | ❌ | 任务广场列表 |
| `/api/tasks/mine` | GET | ✅ | 我发布的任务 |
| `/api/tasks/:id` | GET | ❌ | 任务详情 |
| `/api/tasks/:id/cancel` | POST | ✅ | 撤销任务(退积分) |
| `/api/points/balance` | GET | ✅ | 当前余额 |
| `/api/points/history` | GET | ✅ | 积分流水分页 |

**任务类型配置**(写在 `tasks.js` 的 `TASK_TYPES`):

| 类型 | 单价区间 | 备注 |
|------|---------|------|
| like(点赞) | 1-20 积分 | |
| listen(播放) | 1-30 积分 | 必填 `minListenSec`,15-600 秒 |
| comment(评论) | 3-50 积分 | 可选 `commentRule`(关键词等) |
| share(分享) | 2-30 积分 | |

**关键决策**(下一个 Claude 必读):

1. **分享文案解析正则**:`/《(.+?)》@汽水音乐\s+(https:\/\/qishui\.douyin\.com\/s\/([A-Za-z0-9]+)\/?)/`
   - 短码长度 6-12 位,留余量
   - 校验整段连续匹配,防止"歌名@汽水音乐"配上别的域名

2. **song_id 提取是多策略 fallback**:
   - og:* meta → JSON-LD → window.__INITIAL_STATE__ → window.__SSR_DATA__ → __NUXT__ → RENDER_DATA
   - 抓不到时降级用 `fallback_<shareCode>` 占位,songs.qishui_song_id 不为空保住唯一约束
   - **真实部署后,用户跑通一次发布,把 HTML 片段贴回来,我可以精修提取规则**

3. **同 song_id 在 24 小时内只允许一个活跃任务**:
   - 用 `songs.hasActiveTaskWithinHours(songId, 24)` 校验
   - 拒绝时提示用户「等待该任务结束或撤销」

4. **平台抽成 10%,Math.ceil 向上取整**:
   - 发布者支付:`reward × quota + ceil(reward × quota × 0.1)`
   - 接单者拿:`reward`
   - 平台收到的 fee 等价于"销毁"(没有平台账户,直接从总流通量扣除)

5. **积分扣款用 `SELECT FOR UPDATE` 行锁**:
   - `points.deductInTx` 在事务内先锁行,再校验余额、扣减、记流水
   - 防并发场景下的"双花"

6. **撤销退款按剩余比例**:
   - `usedReward = (quota_total - quota_remaining) × reward`
   - `refund = remaining × reward + (platform_fee - usedFee)`
   - 已被人接的部分不退,平台抽成按已用比例扣除

7. **任务过期默认 7 天**:`TASK_EXPIRE_DAYS = 7`

8. **listen 任务额外校验**:`minListenSec` 不能超过 `songs.duration_sec`(如果抓到了)

9. **链接失效检查机制预留**:
   - tasks 表有 `last_link_check_at` 和 `link_check_failed` 字段
   - 实际定时任务(每小时跑一次)还没实现,等阶段 4-5 一起做

10. **HTTP 抓取的 User-Agent 用的是 iPhone Safari**,因为汽水分享页对桌面 UA 可能返回简化版

**测试情况**:
- 阶段 3 单测全部通过(24 项断言):文案解析 / og 标签 / JSON-LD / __INITIAL_STATE__ / 损坏 JSON 不崩 / 成本计算 / 参数校验
- 沙箱里没有 MariaDB,DB 相关逻辑只能在用户的宝塔环境实测
- 启动测试通过:所有路由和模块加载成功

**部署补充**:
- 阶段 3 不需要任何新的环境变量
- 升级流程:`git pull && pm2 restart qishui-mutual-promotion`
- 注意:**部署后第一次发布任务,会知道 qishui-parser 的提取规则准不准**,可能要调

**待办**(交给下一个 Claude / 阶段):
- 阶段 4:接单 + 凭证上传(图片压缩 + OCR)
- 阶段 5:自动验证(点赞数差值 + 进度时间)
- 中间件:发现 qishui-parser 解析失败率高时,调整提取规则

**注意事项**(下一个 Claude):
- 用户实际部署后,**第一件事**让他试一次完整的「发布任务」流程,看 `/api/tasks/preview` 返回的 `song.qishuiSongId` 是不是 `fallback_xxx` 开头
- 如果是 fallback,说明 HTML 提取规则没命中,需要让用户贴一段 `https://qishui.douyin.com/s/xxx/` 的 HTML 给你,精修 `qishui-parser.extractSongMeta`
- 撤销退款的算法有点绕,在 `tasks.cancelTask`,改之前先看测试用例

### v0.2.0 — 项目骨架 + 注册/登录/邮箱认证(2026-05)

commit:`feat: 阶段2 用户注册 + 邮箱认证 + 临时邮箱屏蔽`

**技术栈定案**:
- 后端:**Node.js 22 + Express 4.21**(对齐 truth-dare-wheel 偏好)
- 数据库:**MySQL 8.0 / MariaDB 10.11**
- 邮件:**QQ 邮箱 SMTP**(smtp.qq.com:465,需 IMAP/SMTP 授权码)
- 密码:**bcrypt**(默认 10 轮)
- 部署:PM2 + 宝塔,端口 **3201**

**项目结构**:
```
qishui-mutual-promotion/
├── src/
│   ├── server.js              ← 入口
│   ├── config/
│   │   ├── index.js           ← 环境变量集中加载 + 启动校验
│   │   └── db.js              ← MySQL 连接池
│   ├── services/
│   │   ├── mailer.js          ← QQ SMTP 发邮件
│   │   ├── email-verification.js  ← 验证码生成 + 校验 + 防刷
│   │   └── auth.js            ← 注册、登录、token 管理
│   ├── routes/
│   │   └── auth.js            ← /api/auth/* 路由
│   ├── middlewares/
│   │   ├── auth.js            ← requireAuth / optionalAuth
│   │   └── rate-limit.js      ← 通用/严格/登录三档限流
│   └── utils/
│       ├── crypto.js          ← bcrypt + SHA256 + 验证码 + token
│       ├── validate.js        ← 邮箱/密码/昵称/验证码格式校验
│       └── request.js         ← IP 提取(支持 X-Forwarded-For)+ UA
├── db/schema.sql              ← 13 张表
├── scripts/
│   ├── init-db.js             ← 一键建库
│   └── import-disposable-domains.js  ← 拉 GitHub 黑名单
├── ecosystem.config.js        ← PM2 配置
├── .env.example               ← 环境变量模板
└── package.json
```

**API 清单**(本版本完成):
| 路径 | 方法 | 鉴权 | 限流 | 说明 |
|------|------|------|------|------|
| `/api/health` | GET | ❌ | 通用 | 健康检查(连 DB) |
| `/api/auth/send-code` | POST | ❌ | 严格 5/分 | 发邮箱验证码 |
| `/api/auth/register` | POST | ❌ | 严格 5/分 | 注册(需先收到验证码) |
| `/api/auth/login` | POST | ❌ | 登录 10/15分 | 登录 |
| `/api/auth/logout` | POST | ❌ | 通用 | 登出 |
| `/api/auth/me` | GET | ✅ | 通用 | 当前用户信息 |

**关键决策**(下一个 Claude 必读):

1. **token 方案是内存 Map**,重启失效,7 天过期。后续上量后切 Redis。
2. **临时邮箱黑名单种子数据 12 条**,部署时**必须跑** `npm run import-disposable` 拉完整 6000+ 列表。
3. **IP 哈希用 SHA256 + 盐**(`IP_HASH_SALT` 环境变量),不存原始 IP。生产环境务必把盐改成随机字符串。
4. **临时邮箱在三个地方都校验**:`send-code` 和 `register` 都校验,防止跳过验证码直接调注册。
5. **邮箱重置密码不返回"用户不存在"**,统一返回"若邮箱存在,验证码已发送",防账号枚举。
6. **昵称允许中文 + 英文 + 数字 + 下划线**,1-20 字符。
7. **密码要求**:8-64 位,至少一字母 + 一数字。
8. **注册时同时记积分流水**:`points_log` 表插入 `register_bonus` +100,与 users.points 同事务。
9. **trust proxy 设为 1**:配合 nginx 反代,否则 IP 全是 127.0.0.1。
10. **请求体限制 1MB**,后续凭证上传走单独 multipart 路由。

**部署步骤**(给用户参考):
```bash
# 服务器上(宝塔目录如 /www/wwwroot/qishui-mutual-promotion)
git clone https://github.com/qiaodaxian233/qishui-mutual-promotion.git
cd qishui-mutual-promotion
npm install
cp .env.example .env
vim .env                          # 填 DB 密码、QQ SMTP 授权码、IP_HASH_SALT
npm run init-db                   # 一键建表(13 张)
npm run import-disposable         # 拉 6000+ 临时邮箱黑名单
pm2 start ecosystem.config.js
pm2 logs qishui-mutual-promotion  # 看启动日志
```

**待办**(交给下一个 Claude / 阶段):
- 阶段 3:任务发布(分享链接解析)
- 阶段 4:接单 + 凭证上传(图片压缩 + OCR)
- 上线前要把 token 切 Redis,防止重启全员掉线

**注意事项**(下一个 Claude):
- 沙箱里 MariaDB 起不来(systemd / 容器限制),代码改完跑 `node --check` + 写单元测试验证,真实测试在用户的宝塔环境做
- 测试方式:本对话用 `env DB_PASSWORD=x SMTP_USER=x SMTP_PASS=x IP_HASH_SALT=xxxx node src/server.js` 验过模块加载、Express 启动监听都成功
- **代码里没有任何敏感信息**(密码、API key 等),全部走 `.env`,gitignore 已排除

### v0.1.0 — 项目立项 + 规则确定(2026-05)

**本阶段产出**(纯文档,无代码):
- 产品定位:独立音乐人互推,非刷量工具
- 9 条核心规则拍板(见上方表格)
- MVP 任务类型确定:点赞 + 播放(P0),评论 + 分享(P1)
- 技术栈方向初定,具体框架待选

**待办**:
- 创建 GitHub 仓库 `qishui-mutual-promotion`(用户操作)
- 初始化 .gitignore + README + 本文件
- 数据库表结构 SQL(下一步)

**注意事项**(下一个 Claude):
- 用户额度焦虑明显,每做完一块立刻推送
- 仓库还没建,等用户确认仓库地址后再开始写代码
- 技术栈(Node vs Go、MySQL vs PostgreSQL)需要再和用户确认

---

*最后更新:v0.7.3 — 邀请奖励调整(1000 → 50 积分)*
*由 Claude (claude-opus-4-7) 与 qiaodaxian233 协作生成*
